[push]
	default = simple
[alias]
	s = status
	a = add
	pl = pull --rebase
	ps = push
	o = checkout
	b = branch
	l = log
	c = commit
	save = !git add . && git commit -m "Save" --no-verify
	unsave = "reset HEAD~"
